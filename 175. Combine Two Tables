# Write your MySQL query statement below
#first name, lastName, city and the state
SELECT
    firstName, lastName, city, state
FROM Person
LEFT JOIN Address ON Person.personId = Address.personId 
ORDER BY firstName
